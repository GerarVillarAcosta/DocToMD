# test_routes.py — obsolete since the desktop migration removed FastAPI.
# HTTP endpoint tests were replaced by test_core.py (hardware/setup_wizard)
# and test_converters.py (conversion engines).
#
# This file is kept empty so git history shows the intentional removal.
